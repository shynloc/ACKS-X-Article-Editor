# 第三方组件与素材

应用使用 React、Vite、TypeScript、CodeMirror、Dexie、unified/remark、Ajv、fflate、Shiki 和 Phosphor Icons。以锁文件中各包附带的 LICENSE 为准；发布归档应保留相应许可证。

正文衬线字体使用 Fontsource 分发的 Noto Serif SC，按 SIL Open Font License 使用。字体从应用自身域名加载，不向 Google Fonts 发起运行时请求。

示例封面使用内置图像生成工具根据已选视觉稿生成，素材只用于演示文稿，不包含用户真实照片。设计参考图由同一流程生成，不能作为实际运行验收截图。

X 名称及相关平台商标属于其各自权利人。项目不是 X 官方产品，不承诺任何会员资格、API 授权、费用或线上渲染结果。
